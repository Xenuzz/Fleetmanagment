{
  "name": "api-backend",
  "version": "1.0.0",
  "description": "FastAPI backend for Fleet Management",
  "main": "app/main.py",
  "scripts": {
    "dev": "uvicorn app.main:app --reload",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000"
  },
  "dependencies": {
    "fastapi": "^0.104.0",
    "uvicorn": "^0.24.0",
    "sqlalchemy": "^2.0.23",
    "pydantic": "^2.5.0",
    "python-dotenv": "^1.0.0",
    "psycopg2-binary": "^2.9.9"
  }
}